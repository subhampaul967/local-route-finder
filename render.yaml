services:
  - name: local-route-finder-backend
    type: web
    env: node
    root: backend
    buildCommand: npm run build
    startCommand: npm run start
    cache:
      - node_modules
      - ~/.npm
      - backend/dist
