# Shared Package

Type-level utilities and shared contracts for the Local Bus / Shared Auto Route Finder.

This package intentionally only provides TypeScript types and interfaces that are imported using `import type` so it does not affect runtime bundling.
